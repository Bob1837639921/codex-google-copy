# Legacy Xiaohongshu Examples

These scripts are retained as implementation references only. They are not the production execution path and must not be selected by an agent for routine browsing.

Use the generic NodeX MCP tools with `skills/xiaohongshu-automation/SKILL.md`. The extension now enforces Xiaohongshu pacing and the runtime stops on `blocked_by_login` or `blocked_by_risk`.

Do not turn these examples into exhaustive crawlers, parallel-tab workers, CAPTCHA solvers, fingerprint modifiers, or retry loops.
